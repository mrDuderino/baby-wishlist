export const STORAGE_BUCKET = "wishlist" as const;

export const REALTIME_CHANNELS = {
  products: "products",
  reservations: "reservations",
  settings: "settings",
} as const;

export const ADMIN_ROUTE_PREFIX = "/admin" as const;
